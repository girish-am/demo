name: Github actions demo
on: [push]

jobs:
    print-all-env:
        runs-on: ubuntu-latest
        steps:
            - name : print all envs 
              run: echo "GITHUB_ACTION = ${{ GITHUB_ACTION }}"
                  
        

            